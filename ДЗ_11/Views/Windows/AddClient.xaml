<Window x:Class="ДЗ_11.Views.Windows.AddClient"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.fontawesome.com/icons/"
        xmlns:local="clr-namespace:ДЗ_11.Views.Windows"
        mc:Ignorable="d"
        Title="Новый клиент" Height="400" Width="250"
        xmlns:view="clr-namespace:ДЗ_11.ViewModels"
        WindowStartupLocation="CenterScreen"
        Background="White">
    <Window.DataContext>
        <view:AddClientViewModel/>
    </Window.DataContext>
    <StackPanel Orientation="Vertical" Margin="15">
        <Label Content="Фамилия"/>
        <TextBox Text="{Binding NewClient.LastName}"/>
        <Label Content="Имя"/>
        <TextBox Text="{Binding NewClient.Name}"/>
        <Label Content="Отчество"/>
        <TextBox Text="{Binding NewClient.Patronymic}"/>
        <Label Content="Телефон"/>
        <TextBox Text="{Binding NewClient.PhoneNumber}"/>
        <Label Content="Серия и номер паспорта"/>
        <TextBox Text="{Binding NewClient.Passport}"/>
        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="53*"/>
                <ColumnDefinition Width="57*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="2" Padding="1" Command="{Binding CreateNewClientCommand}">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <fa:ImageAwesome Icon="Regular_Save" Height="15" Margin="0,0,4,0"/>
                        <TextBlock Text="Сохранить" FontSize="12"/>
                    </StackPanel>
                </Button.Content>
            </Button>
            <Button Grid.Column="1" Margin="2" Padding="1" Background="Red">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <fa:ImageAwesome Icon="Regular_WindowClose" Height="15" Margin="0,0,4,0"/>
                        <TextBlock Text="Отмена" FontSize="12"/>
                    </StackPanel>
                </Button.Content>
            </Button>
        </Grid>
       
    </StackPanel>
</Window>
